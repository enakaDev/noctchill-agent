# 🎵 ノクチル マルチエージェント開発 - 実装前準備ガイド

**作成日**: 2026年2月5日  
**対象**: Phase 2（Claude Code エージェント実装）準備中

---

## 📌 現在のプロジェクト状態

✅ **Phase 1 完了**: 基本構造・ドキュメント完成  
🔄 **Phase 2 準備中**: Claude Code エージェント実装前

---

## 🎯 Phase 2 実装開始前にやることリスト

### □ 1. 前提環境の準備

#### WSL2 環境確認
```bash
# WSL バージョン確認
wsl --version

# Ubuntu バージョン確認
lsb_release -a
```

#### Node.js 確認
```bash
# Node.js バージョン確認（16以上推奨）
node --version
npm --version
```

#### Claude Code インストール確認
```bash
# Claude Code がインストールされているか確認
which claude

# インストール（必要な場合）
npm install -g @anthropic-ai/claude-code
```

#### tmux インストール確認
```bash
# tmux バージョン確認
tmux -V

# インストール（必要な場合）
sudo apt-get update
sudo apt-get install tmux
```

### □ 2. プロジェクトの基本操作習熟

#### YAML ファイル形式の理解
- `queue/producer_to_manager.yaml` - 指示フォーマット
- `queue/tasks/asahara.yaml` など - タスクフォーマット
- `queue/reports/asahara_report.yaml` など - 報告フォーマット

YAML の基本書き方：
```yaml
key: value                    # 基本
nested:
  child_key: child_value      # ネスト
array:
  - item1
  - item2                     # 配列
multiline: |                  # 複数行
  これは
  複数行のテキストです
```

#### tmux の基本操作習熟
```bash
# セッション操作
tmux new-session -s name
tmux attach-session -t name
tmux kill-session -t name

# ウィンドウ操作
tmux new-window -t session
tmux select-window -t session:0

# ペイン操作
tmux split-window -h    # 水平分割
tmux split-window -v    # 垂直分割
tmux send-keys -t session:pane "command" Enter
```

#### ファイルシステム理解
- 各ロール用指示書の役割
- ディレクトリ構成
- ファイルの読み書きフロー

### □ 3. Claude Code 使用方法の習熟

#### Claude Code 起動
```bash
# Claude Code REPL 起動
claude

# または
claude-code
```

#### Claude Code で使用可能なコマンド
```bash
# ファイル読み込み
@read path/to/file

# ファイル書き込み
@write path/to/file
content...

# ディレクトリ構造確認
@ls path/to/directory

# コマンド実行
bash: command here

# 複数行コマンド
bash: |
  command1
  command2
```

#### プロンプト入力のコツ
- 詳細な背景情報を提供
- 期待される出力形式を明確に
- エラーハンドリング方法を指示
- ロール設定プロンプトを活用

### □ 4. 役割定義書の深い理解

#### マネージャー用
- `instructions/manager.md` を熟読
- タスク分解ロジック理解
- 報告集約方法理解

#### 各アイドル用
- `instructions/asahara.md` - リーダーシップ
- `instructions/higuchi.md` - 創意工夫
- `instructions/fukumaru.md` - 品質管理
- `instructions/ichikawa.md` - 実装支援

---

## 💻 Phase 2 実装ステップ

### ステップ 1: マネージャーエージェント実装

#### 1.1 Claude Code セッション起動
```bash
# WSL2 ターミナル
cd /home/shoot/work/noctchill-agent

# Claude Code 起動
claude
```

#### 1.2 プロンプト入力
`scripts/manager_prompt.txt` の内容をコピーして、Claude に貼り付けます。

#### 1.3 初期テスト実行
```yaml
# queue/producer_to_manager.yaml
task_id: "init_test"
command: "システム動作確認"
description: "各エージェントが正常に起動できることを確認"
```

#### 1.4 動作検証
- ✅ マネージャーが指示を受け取る
- ✅ タスクファイルが生成される
- ✅ 報告ファイルが生成される
- ✅ ダッシュボード更新される

### ステップ 2: 各アイドルエージェント実装

#### 2.1 アイドル起動スクリプト作成
```bash
# scripts/ に各アイドル用スクリプト作成
scripts/idol_asahara.sh
scripts/idol_higuchi.sh
scripts/idol_fukumaru.sh
scripts/idol_ichikawa.sh
```

#### 2.2 各アイドルの Claude Code セッション起動
```bash
# tmux pane 0 で
claude < scripts/idol_asahara.sh

# tmux pane 1 で
claude < scripts/idol_higuchi.sh

# ... 以下同様
```

#### 2.3 通信テスト
- ✅ マネージャー → アイドル タスク配信
- ✅ アイドル → マネージャー 報告送信
- ✅ 4つが並列実行できることを確認

### ステップ 3: 統合テスト

#### 3.1 全エージェント起動
```bash
# tmux セッション確認
tmux list-sessions

# 全ウィンドウ起動確認
tmux list-windows -t noctchill
```

#### 3.2 簡単なタスク実行
```yaml
task_id: "integration_test"
command: "全員テストファイル作成"
description: "各メンバーが test_output/ に自分の名前のファイルを作成"
```

#### 3.3 ダッシュボード確認
```bash
# ダッシュボード監視
tail -f status/dashboard.md
```

---

## 📚 参考リソース

### Claude Code ドキュメント
- [Anthropic Claude API Docs](https://docs.anthropic.com)
- Claude Code 公式ドキュメント

### tmux チートシート
- tmux コマンド参考：多くのブログで「tmux チートシート」で検索
- 実践的な例：このプロジェクトの scripts/ ディレクトリ

### YAML 形式
- YAML 公式サイト：https://yaml.org/
- オンラインエディタ：YAML 簡易検証ツール

### 参考プロジェクト
- [元記事コード](https://github.com/yohey-w/multi-agent-shogun)
- 記事内で紹介されている GitHub リンク

---

## 🎓 学習ポイント

### マルチエージェント設計
- 階層構造（producer → manager → idols）
- 非同期通信（ファイルベース）
- エラーハンドリング

### Claude Code 活用
- ロール設定（プロンプトによるペルソナ設定）
- ファイル I/O の活用
- 並列処理の実装

### システム設計
- YAML による構造化通信
- イベント駆動アーキテクチャ
- ダッシュボード自動更新

---

## ⚠️ よくある落とし穴

### 落とし穴 1: tmux send-keys 2回分割ルール
❌ `tmux send-keys -t pane "msg" Enter`  
✅ `tmux send-keys -t pane "msg"` + `tmux send-keys -t pane Enter`

### 落とし穴 2: ファイルパス指定
- 相対パス：`queue/producer_to_manager.yaml`
- プロジェクトルートからの相対パス使用

### 落とし穴 3: YAML 形式エラー
- インデント 2 文字で統一
- タブは使用不可
- コロン後に半角スペース

### 落とし穴 4: エージェント間通信の混同
- 各エージェントは**自分のファイルのみ**処理
- 他のファイルを見たら「違反は切腹」
- ファイル名を明確に分ける

---

## ✅ チェックリスト（実装開始前）

- [ ] WSL2 + Ubuntu 環境確認
- [ ] Node.js 16+ インストール確認
- [ ] Claude Code インストール済み
- [ ] tmux インストール済み
- [ ] YAML 形式理解
- [ ] tmux 基本操作習熟
- [ ] 役割定義書全て熟読
- [ ] scripts/ のスクリプト理解
- [ ] Git コマンド理解（add, commit など）
- [ ] このガイドを全て読了

---

## 🚀 Phase 2 実装スケジュール案

| 日付 | 内容 | 目標 |
|------|------|------|
| 2/6-2/8 | マネージャーエージェント実装 | 指示受け取り・タスク分配 |
| 2/9-2/11 | 各アイドルエージェント実装 | 各自がタスク処理・報告 |
| 2/12-2/14 | 統合テスト・デバッグ | 全員が正常に動作 |
| 2/15 | Phase 2 完了 | マルチエージェント完成 |

---

## 📞 質問・困ったときは

### デバッグのコツ
1. ダッシュボード確認：進捗状況を確認
2. ログファイル確認：エラーメッセージを確認
3. YAML 形式確認：形式エラーの確認
4. ファイルパス確認：ファイルが存在するか確認

### よくある質問

**Q. Claude Code が起動しない**
```bash
A. npm install -g @anthropic-ai/claude-code で再インストール
```

**Q. tmux が起動しない**
```bash
A. sudo apt-get install tmux で tmux をインストール
```

**Q. ファイルが見つからない**
```bash
A. pwd で現在位置確認、cd で移動
```

---

## 🎊 完成後の姿

実装完了後のイメージ：

```
🎵 ノクチル がんばって！

[Manager] 「マスター、タスク受け取りました！」
           ↓
[Asahara] ✅ タスク完了
[Higuchi] ✅ タスク完了
[Fukumaru] ✅ タスク完了
[Ichikawa] ✅ タスク完了
           ↓
[Manager] 「全員完了しました。ダッシュボード更新いたします。」
           ↓
[Dashboard] 📊 リアルタイム表示
[Smartphone] 📱 スマホからも確認可能
```

---

**Phase 2 実装開始予定**: 2026年2月中旬  
**目標完了日**: 2026年2月15日
